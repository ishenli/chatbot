package com.workdance.chatbot.ui.demo;

import androidx.appcompat.app.AppCompatActivity;

import com.workdance.chatbot.R;

public class HeadZoomLayoutActivity extends AppCompatActivity {
    public void onCreate(android.os.Bundle savedInstanceState)
    {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_head_zoom);
        // ImageView imageView = findViewById(R.id.iv_image);
        //
        // Glide.with(imageView.getContext())
        //         .load("https://file.moyublog.com/free_wallpapers_files/5j5v43f0sj1.jpg")
        //         .into(imageView);
    }
}
